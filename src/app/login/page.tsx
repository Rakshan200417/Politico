import LoginForm from "@/components/login/LoginForm";

export const metadata = {
  title: "Log in | POLITICO",
  description: "Log in to your POLITICO account.",
};

export default function LoginPage() {
  return <LoginForm />;
}
